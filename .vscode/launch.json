{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "TRM.CMD (Debug)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build TRM.CMD",
            "program": "${workspaceFolder}\\TRM.CMD\\bin\\Debug\\net10.0\\TRM.CMD.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "console": "integratedTerminal"
        }
    ]
}