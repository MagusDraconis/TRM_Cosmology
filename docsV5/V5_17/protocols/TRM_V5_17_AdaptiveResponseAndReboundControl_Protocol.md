# ARP Protocol — Adaptive Response and Rebound Control
Baseline: M3+ (frozen). Test: two-stage intervention: probe → measure rebMagnitude → apply second intervention if rebMagnitude low. No new pathways. No threshold tuning on holdout.
