# TRM Collective Mode Locking BridgeScale

## Problem: EulerBridgeScale

The bridge parameter `EulerBridgeScale` was previously discussed as a near-single value around `0.85`.
The current question is no longer "can 0.85 be fitted?", but whether the bridge scale is better described by a collective rational locking structure.
This parameter was critical because it directly controls whether the EL/Fermat photon path remains inside a physically acceptable weak-field window.

---

## EL01-EL17: Bridge Track

- EL01–EL17 establish an executable EL/Fermat bridge track in the weak-field regime.
- The bridge path is deterministic and test-backed.
- EL17 marks the emergence boundary: removing cadence support reduces 20/17 competitiveness.

Status:

> `EulerBridgeScale ≈ 0.85` is validated as prior-assisted in the current bridge track, not yet first-principles emergent.

---

## CML01-CML08: Isolated Mode-Locking Track

- CML01-CML04 establishes isolated collective mode-locking behavior without `PhotonTransportModel`.
- CML05 removes explicit cadence prior from the score (`0.55*meanOrder + 0.45*alignment`).
- CML06-CML07 shows a competitive rational cluster/band in \(\Omega \approx 1.16..1.19\).
- CML08 maps the competitive band via `EulerBridgeScale = 1/Omega` into EL/Schwarzschild weak-field consistency.

Observed CML08 summary:

- Competitive band count: `10`
- EL ratio window from band mapping: `0.8825803 .. 1.226584` (inside the test window `0.85 .. 1.25`)

---

## Main result: rational band instead of single resonance

The Euler bridge scale is no longer best described as a single fitted value.
It is supported by an isolated rational collective mode-locking band whose inverse cadence maps into the weak-field EL/Fermat bridge window.
The exact selection of a unique cadence within that band remains open.

\[
\Omega \approx 1.16..1.19
\Rightarrow
\gamma = 1/\Omega \approx 0.84..0.86
\]

`20/17` is therefore treated as a representative candidate inside the band, not as a uniquely fundamental constant.

---

## Current boundary

What is established:

- A rational collective locking band can produce bridge-scale values in the validated weak-field EL window.

What is not established yet:

- A first-principles derivation that uniquely selects one cadence (for example exactly `20/17`) from inside that band.

---

## Next theory question

\[
\boxed{\text{Why does this specific rational locking band emerge?}}
\]

This replaces the narrower question "does 0.85 work?" with a stronger collective-selection problem.

---

## Peer-review-safe wording

CML08 shows that the EL bridge scale is consistent with an inverse rational collective mode-locking band. The value gamma ≈ 0.85 should therefore not be presented as an isolated fitted constant, but as a representative point inside a robust collective locking window. The first-principles selection of the band remains open.
