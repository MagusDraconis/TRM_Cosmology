# TRM M3 RBF27–RBF31 Action-Derived Boundary Note

## 1. Result summary (RBF27–RBF31)

- **RBF27** supports strong agreement between the derived action/tick discriminator and the prior operational discriminator.
- **RBF28** shows that under the shared full three-constraint stack, the baseline derived rule selects `m=3` as first/full-stack admissible mode.
- **RBF29** maps a bounded admissibility region and transition behavior under phase-stress scanning near the `phaseThr≈0.785–0.790` regime.
- **RBF30** confirms baseline shared-rule exclusion behavior for competing families under the derived three-constraint rule.
- **RBF31** explains `m=2` fallback as a phase/action boundary tradeoff, not as random selection noise.

---

## 2. Updated status

Reviewer-safe status upgrade:

> `m=3` is supported as an **action-derived bounded three-constraint bridge-mode candidate**.

This is an upgrade from a purely bounded diagnostic candidate path toward an action-derived bounded candidate path.

---

## 3. Boundary and fallback interpretation

The combined RBF26/RBF29/RBF31 picture is:

1. `m=3` is selected within a bounded three-constraint admissibility region.
2. A transition boundary appears under stronger phase stress around `phaseThr≈0.785–0.790`.
3. `m=2` fallback at/after boundary is explained by phase/action tradeoff structure in the same derived shared-rule model.

Interpretation:

> The fallback is a boundary effect of the constrained model class, not a direct contradiction of bounded `m=3` support.

---

## 4. Claim boundaries

- candidate / diagnostic / tested-effective only
- not theorem-level proof
- not universal `m=3` selection claim
- not first-principles closure
- no GR replacement claim
- no numerology claim

---

## 5. Next test path toward RBF32+

1. **RBF32**: derive a stricter action/tick functional directly from the V3.2 minimal lattice-action path with explicit no-retuning guard.
2. **RBF33**: boundary-manifold refinement around the phase/action transition with denser deterministic grids and uncertainty-style margin reporting.
3. **RBF34**: cross-window transfer test (`q`-window families) under one frozen derived rule.
4. **RBF35**: competing-family exclusion stress test with alternative admissible lattice-action variants under explicit penalty discipline.
5. **RBF36**: consolidated bounded-domain statement test (selection + boundary + fallback explanation in one shared report format).

Target remains claim-safe:

> stronger action-derived bounded candidate discipline, still below theorem-level first-principles closure.
